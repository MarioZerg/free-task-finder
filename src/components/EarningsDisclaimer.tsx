import Icon from '@/components/ui/icon';

/** Оговорка для страниц о подработке.
 *
 *  Директ проверяет посадочную вместе с объявлением: в рекламе заработка
 *  нельзя обещать доход, гарантировать заказы и утверждать, что риска нет.
 *  Текст закрывает эти пункты и заодно честно объясняет человеку, как
 *  устроен сервис. Держим одним компонентом, чтобы формулировка на всех
 *  страницах совпадала — расхождения модерация замечает. */
const EarningsDisclaimer = ({ className = '' }: { className?: string }) => (
  <p
    className={`flex items-start gap-2.5 rounded-2xl border border-line bg-tile p-4 text-sm text-muted-foreground ${className}`}
  >
    <Icon name="Info" size={16} className="mt-0.5 shrink-0" />
    <span>
      Доделай.ру — доска объявлений. Сервис не является работодателем,
      не гарантирует наличие заказов и не участвует в расчётах: условия,
      стоимость и сроки исполнитель обсуждает с заказчиком напрямую.
    </span>
  </p>
);

export default EarningsDisclaimer;
