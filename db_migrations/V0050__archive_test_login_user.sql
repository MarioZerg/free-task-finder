-- Тестовый профиль, созданный при проверке входа через MAX, убираем
-- из выдачи штатным способом: сервис не показывает архивные записи.
UPDATE users
   SET role = 'archived',
       erased_at = NOW(),
       name = 'Удалённый профиль'
 WHERE max_id = 'testflow';
